* Can we use a `stdev` for the threshold values instead of `0.6`?
* What does `PMCM` mean?
* First method used the average of a bunch of messages, the second used the average of a few terms that indicated maintenance